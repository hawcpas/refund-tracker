import 'package:flutter/material.dart';

class AppColors {
  // Primary page background (light)
  static const Color pageBackgroundLight =
      Color.fromARGB(255, 220, 220, 220);
  
  // Alternate page background (dark / brand)
  static const Color pageBackgroundDark =
      Color(0xFF0B346A);

  // Light Blue color
  static const Color lightGrey = 
      Color(0xFF667085);

  // Brand color
  static const Color brandBlue =
      Color(0xFF08449E);

  // Card color
  static const Color cardBackground =
      Colors.white;

  // Footer / secondary text
  static const Color mutedText =
      Color.fromARGB(255, 66, 66, 66);
}
